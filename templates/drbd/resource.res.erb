resource <%= name %> {
  device /dev/drbd_<%= name %> minor <%= minor %>;
  disk   <%= disk %>;
  meta-disk internal;
  on <%= mastername %> {
    address <%= masterip %>:<%= port %>;
  }
  on <%= slavename %> {
    address <%= slaveip %>:<%= port %>;
  }
}
